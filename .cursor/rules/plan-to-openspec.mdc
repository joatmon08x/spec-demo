---
description: Spec work starts with /opsx-explore, not Cursor Plan mode
alwaysApply: true
---

# OpenSpec starts at explore

Do not use Cursor Plan mode or `/plan` for spec-driven work. Start with `/opsx-explore`, then `/opsx-propose` → `/opsx-apply` → `/opsx-archive`. Do not edit product code until an OpenSpec change exists under `openspec/changes/<id>/`.

```
# ❌ BAD
/plan enable Accept and Decline on the dispute Resolution panel

# ✅ GOOD
/opsx-explore enable Accept and Decline on the dispute Resolution panel
/opsx-propose
```

Cursor Desktop spelling is hyphenated (`/opsx-explore`). After propose, implement with `/opsx-apply` against the accepted delta specs. Do not archive unless asked.

Spec-track issues: Linear project [openspec](https://linear.app/anysphere/project/openspec-05fc3d7dba89) (LY-6, LY-7, LY-8) on repo [joatmon08x/spec-demo](https://github.com/joatmon08x/spec-demo). One OpenSpec change per issue after `/opsx-explore`. Cloud Agent roles and closeout: `openspec/sdk-kickoff.md`.
